{
  "name": "phrolova-monorepo",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "client:dev": "pnpm --filter client dev",
    "client:build": "pnpm --filter client build",
    "client:preview": "pnpm --filter client preview"
  }
}
